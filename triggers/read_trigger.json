{
    "type": "shortcut",
    "name": "Read trigger",
    "description": "Read your triggers",
    "workflow": "#/workflows/read_workflow",
    "inputs": {
        "interactivity": {
            "value": "{{data.interactivity}}"
        },
        "channel": {
            "value": "{{data.channel_id}}"
        }
    }
}
